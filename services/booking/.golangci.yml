version: "2"
linters:
  enable:
    # Базовые
    - errcheck
    - govet
    - staticcheck
    - ineffassign

    # Безопасность и качество
    - gosec
    - revive
    - misspell
    - unconvert

    # Дополнительные
    - bodyclose
    - prealloc
    - errorlint

  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (*database/sql.Tx).Rollback

    govet:
      enable:
        - fieldalignment

    revive:
      rules:
        - name: var-naming
          disabled: true

  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$

formatters:
  enable:
    - gofmt
    - goimports

  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$